auto lo
iface lo inet loopback

auto eth0
iface eth0 inet static
  address {{ ansible_ssh_host }}
  netmask 255.255.255.0
  gateway {{ gateway }}
  dns-domain {{ domain }}
  dns-nameserver {{ bind }}
  dns-search {{ domain }}
